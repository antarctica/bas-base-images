# This is an example Terraform variables file
# This file can be copied as `terraform.tfvars` and updated with real values, all are required
# For real example values see the 'BAS Base Images - Example Terraform variables' entry in the BAS MAGIC 1Password

datacentre      = "datacentre1"
network         = "network1"
storage-cluster = "storage1"
compute-cluster = "cluster1"
resource-pool   = "resource1"
